// Package oauth2client holds the configuration model and registry for OAuth2
// and OpenID Connect identity-provider clients, looked up by provider id,
// email or email domain, and a helper to build token-endpoint requests. It
// does not run the authorization-code flow itself.
package oauth2client
