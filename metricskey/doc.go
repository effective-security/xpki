// Package metricskey defines the metrics.Describe descriptors emitted by the
// crypto providers (perf_crypto) and the certificate authority (perf_ca,
// perf_ca_signreq), and the Metrics list used to register them.
package metricskey
